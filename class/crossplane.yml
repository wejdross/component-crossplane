parameters:
  kapitan:
    compile:
      - input_paths:
          - crossplane/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - crossplane/component/main.jsonnet
        input_type: jsonnet
        output_path: crossplane/

